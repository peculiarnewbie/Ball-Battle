using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraController : MonoBehaviour
{
    [SerializeField] Cinemachine.CinemachineVirtualCamera matchVCam;
    [SerializeField] Cinemachine.CinemachineVirtualCamera fakeMatchVCam;
    [SerializeField] Cinemachine.CinemachineVirtualCamera goalVCam;
    [SerializeField] Cinemachine.CinemachineVirtualCamera startVCam;

    // Start is called before the first frame update
    void Start()
    {
        // startVCam
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
